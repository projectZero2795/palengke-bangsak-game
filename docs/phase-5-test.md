# Phase 5 Test Notes

## Scope

Phase 5 designs the safe local `Bang` action.

Included:

- `BangActionVisualStyle`
  - `FingerMarker`;
  - `ToyDart`;
  - `CartoonLightBeam`;
  - `TsinelasMarker`.
- `BangActionController`
  - safe thrown `tsinelas` marker;
  - red forward-cone range indicator;
  - small impact burst;
  - cooldown;
  - keyboard trigger with `Space`;
  - effect direction based on the current player facing direction;
  - short arc/spin animation for the thrown tsinelas;
  - no damage, no catching, no hit validation.
- `BangActionHud`
  - runtime-created compact circular Bang button;
  - dark button style with gold rim;
  - static tsinelas icon;
  - compact lower-right screen anchoring;
  - cooldown text while the action is unavailable.
- Placeholder assets:
  - `bang_marker_placeholder.png`;
  - `bang_range_placeholder.png`;
  - `bang_impact_placeholder.png`;
  - `bang_button_dark_placeholder.png`;
  - generated by `tools/generate_phase5_bang_assets.py`.
- Default player prefab updates:
  - `BangActionController`;
  - `BangActionHud`;
  - tsinelas marker sprite;
  - forward-cone range indicator sprite;
  - impact burst sprite;
  - circular button background/icon sprites.
- EditMode tests for:
  - safe default tsinelas visual style;
  - cooldown behavior;
  - cooldown progress;
  - 8-direction effect vectors;
  - forward-cone range rotation;
  - effect placement;
  - prefab wiring;
  - `PrototypeMap` playable-player wiring.

Not included:

- projectile physics;
- raycast hit detection;
- player hitboxes;
- caught state;
- Taya/Hider role restrictions;
- score;
- networking;
- Photon;
- WebGL build;
- Docker/Kubernetes/Argo files.

## Safety note

This phase does not implement realistic weapons. `Bang` is represented as a playful thrown `tsinelas` tag marker with a red forward-cone range preview and small non-violent impact burst. The visual-style enum keeps future safe alternatives explicit, including toy dart and tsinelas marker themes.

## Manual Unity Editor checks

1. Pull latest `main`.
2. Fully close and reopen Unity so project settings and prefab changes reload.
3. Open `unity/` in Unity 2022.3.50f1 or newer.
4. Open `Assets/Scenes/PrototypeMap.unity`.
5. Press Play.
6. Confirm a compact circular button appears fully inside the lower-right of the screen, without clipping off the edge.
7. Confirm the button uses the dark/gold circular background and static tsinelas icon.
8. Confirm a red forward cone appears from the player and rotates when the player changes facing direction.
9. Move in any direction and press `Space`.
10. Confirm the tsinelas marker arcs/spins forward in the direction the player is facing.
11. Confirm the small impact burst appears at the end of the throw.
12. Press the circular Bang button.
13. Confirm the cooldown prevents spam and the button displays cooldown text.
14. Confirm the action does not catch, damage, delete, or affect another player.
15. Confirm Console has no red errors.

## Exit criteria status

- Safe tsinelas Bang visual metaphor: ready for review.
- Circular Bang button: ready for review.
- Cooldown: ready for review.
- Forward-cone range indicator: ready for review.
- Effect direction follows facing: ready for review.
- Tsinelas throw animation: ready for review.
- Bang action approval: pending project owner review.
