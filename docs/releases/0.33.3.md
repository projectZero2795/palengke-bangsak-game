# Bang-Sak 0.33.3 signed-in migration release

Released: 2026-07-12

## Changes

- Added the restricted Palengke-to-game signed-in identity bridge.
- Added the exact Palengke frame source to the game CSP.
- Added versioned WebGL asset URLs to prevent mixed-release browser caches.
- Kept `games.palengke.es` and `bangsak.palengke.es` live during acceptance.

## Release references

- final game source: `784cecce2df9323be3bc43eeda5edabb01e12d7f`;
- Palengke source: `919df92a069848d1a55400d0df440cc1217f6b31`;
- version: `0.33.3`;
- Unity: `2022.3.50f1`;
- WebGL artifact: `37,487,735` bytes;
- image: `registry.renzlab.com/palengke/bang-sak:784cecc`;
- image digest:
  `sha256:f5f20290fb78ab91d4620eb1b2298793eafe3d6a29b6d8b0344abe2a9293cb84`;
- final GitOps deployment:
  `b1efcddef22bfae30c7c05fcc07e2a1d4a9ddb63`.

## Verification

- Unity EditMode: `228` passed, `0` failed;
- local container health, security headers, cache-busted asset URLs, Nginx
  syntax, and UID/GID `101:101` passed;
- Argo CD reported `Synced`/`Healthy`, deployment `2/2`, and two Ready replicas
  on separate nodes;
- both public production verifiers passed;
- signed-in identity, two-client Photon, authoritative score `500`, five-coin
  reward, and live leaderboard persistence passed with no browser errors.

## Rollback

Point the Bang-Sak image in `palengke-gitops` back to
`registry.renzlab.com/palengke/bang-sak:92fa807`, push, wait for Argo CD to
report `Synced` and `Healthy`, and rerun both production verifiers.
